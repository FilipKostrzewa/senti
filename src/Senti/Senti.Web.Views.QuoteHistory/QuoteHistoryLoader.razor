@page "/quote-history-loader"

@inject NavigationManager _navigationManager

@code
{
    protected override void OnInitialized()
    {
        _navigationManager.NavigateTo("/quote-history", true);
    }
}
